`make install_dep` - установить зависимости, которые использует этот
проект(aiogram, pydantic-setting, python-dotenv и requests)

`make create_and_install` - создать виртуальное окружение(.venv) и установить зависимости

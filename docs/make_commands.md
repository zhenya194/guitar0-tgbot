`make install_dep` - install dependancies that uses in this
project(aiogram, pydantic-setting, python-dotenv and requests)

`make create_and_install` - create virtual enviroment(.venv) and install dependancies to it
